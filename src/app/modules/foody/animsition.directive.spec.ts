import { AnimsitionDirective } from './animsition.directive';

describe('AnimsitionDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimsitionDirective();
    expect(directive).toBeTruthy();
  });
});
